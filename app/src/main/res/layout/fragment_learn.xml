<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="horizontal"
    android:layout_gravity="center"
    android:gravity="center"
    tools:context="com.github.almasud.augmented_learn.view.fragment.LearnFragment">

    <ImageView
        android:id="@+id/ivLearnItem"
        android:layout_width="200dp"
        android:layout_height="200dp"
        tools:src="@drawable/capital_a"
        android:layout_marginEnd="@dimen/activity_horizontal_margin"
        tools:ignore="ContentDescription" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center">

        <ImageView
            android:id="@+id/ivLearnRealView"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:src="@drawable/ar_view"
            android:scaleType="centerCrop"
            android:clickable="true"
            android:focusable="true"
            app:tint="@color/colorPrimary"
            android:layout_marginBottom="@dimen/activity_half_margin"
            tools:ignore="ContentDescription" />

        <TextView
            android:id="@+id/tvLearnName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            tools:text="@string/alphabet_apple"
            android:textSize="30sp"
            android:textStyle="bold"
            android:padding="@dimen/activity_half_margin"
            android:textAlignment="center"
            android:textColor="@android:color/white"
            android:background="@color/colorPrimary"
            android:layout_marginBottom="@dimen/activity_half_margin"/>

        <ImageView
            android:id="@+id/ivLearnExtra"
            android:layout_width="150dp"
            android:layout_height="150dp"
            tools:src="@drawable/alphabet_apple"
            tools:ignore="ContentDescription"
            android:visibility="gone"/>

    </LinearLayout>

</LinearLayout>